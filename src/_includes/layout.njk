<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sanctuary</title>
    <style>
        body { background-color: #0a0a0a; color: #00ff41; font-family: monospace; padding: 20px; }
        .header-box { border: 1px solid #00ff41; padding: 10px; margin-bottom: 20px; display: flex; justify-content: space-between; }
        #clock { font-weight: bold; }
    </style>
</head>
<body>
    <div class="header-box">
        <div>LOC: LISTUGUJ, QC</div>
        <div id="clock"></div>
    </div>
    {{ content | safe }}
    <script>
        function updateClock() {
            document.getElementById('clock').innerText = new Date().toLocaleTimeString('en-US', { hour12: true });
        }
        setInterval(updateClock, 1000); updateClock();
    </script>
</body>
</html>
