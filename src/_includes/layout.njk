<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
        body {
            background-color: #0a0a0a;
            color: #00ff41;
            font-family: 'Courier New', Courier, monospace;
            padding: 20px;
            line-height: 1.6;
        }
        a { color: #008f11; text-decoration: none; border: 1px solid #008f11; padding: 2px 5px; }
        a:hover { background: #008f11; color: #0a0a0a; }
        .emergency-bar {
            background: #440000;
            color: #ff0000;
            padding: 15px;
            border: 2px solid #ff0000;
            margin-bottom: 20px;
            font-weight: bold;
        }
        .hidden { display: none; }
    </style>
</head>
<body>
    <div id="alert-box" class="emergency-bar">
        ⚠️ EMERGENCY BROADCAST: UNAUTHORIZED SIGNAL DETECTED IN SECTOR 4.
    </div>

    {{ content | safe }}

    <script>
        // After 60,000 milliseconds (1 minute), hide the alert box
        setTimeout(function() {
            document.getElementById('alert-box').classList.add('hidden');
        }, 60000);
    </script>
</body>
</html>
